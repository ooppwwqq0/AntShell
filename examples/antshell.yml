# ssh config
DB_FILE : ~/.ssh/ant.db
ODB_FILE : ~/.ssh/autosshrc
KEY_PATH : ~/.ssh/id_rsa

# not use conf
BACK_DIR : .sshkeybak

# default user info
USER : wangping
PASS : ''
PORT : 22

# zh or en
#langset : zh
